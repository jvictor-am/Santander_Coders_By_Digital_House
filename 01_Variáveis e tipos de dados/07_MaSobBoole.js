// Para continuar, defina duas variáveis: umNumeroPequeno e umNumeroGrande, e
// atribua a elas valores numéricos diferentes de acordo com seus nomes. Então
// defina a variável eMenor e atribua o resultado da comparação se
// umNumeroPequeno é menor do que umNumeroGrande; e defina a variável eMaior,
// com o resultado da comparação umNumeroPequeno é maior que umNumeroGrande.

var umNumeroPequeno = 5;

console.log('umNumeroPequeno: ' + umNumeroPequeno);

var umNumeroGrande = 8;

console.log('umNumeroGrande: ' + umNumeroGrande);

var eMenor = umNumeroPequeno < umNumeroGrande;

console.log('umNumeroPequeno < umNumeroGrande: ' + eMenor);

var eMaior = umNumeroPequeno > umNumeroGrande;

console.log('umNumeroPequeno > umNumeroGrande: ' + eMaior);
