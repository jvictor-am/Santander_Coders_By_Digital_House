// Vamos praticar juntos: escreva agora uma função metade, que pega um número
// como parâmetro e retorna sua metade.

function metade(number) {
  return number / 2;
}

console.log('metade(10): ' + metade(10));
